[% if notebook is defined %]
-- Create battery widget
myassault = assault({
            width = 12,
            height = 7,
            bolt_width = 8,
            bolt_height = 7,
	    battery = '[[% notebook.ps_bt_string | default('BAT0') %]]',
	    adapter = '[[% notebook.ps_ac_string | default('AC') %]]',
})
[% endif %]
